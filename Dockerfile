FROM ubuntu:14.04

RUN \
  sed -i 's/# \(.*multiverse$\)/\1/g' /etc/apt/sources.list && \
  apt-get update && \
  apt-get install -y mono-runtime

ADD . /publish

EXPOSE 8123

CMD ["mono","HelloWorld.exe"]
